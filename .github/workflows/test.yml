name: Test
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    permissions:
      contents: read
      id-token: write
      security-events: write
    uses: Wuodan/bats-.github/.github/workflows/test.yml@v1
